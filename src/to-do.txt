// Property name validation

 - including mapping

 Method parameter's must have matching property names, unless:

 - AggregateProperty contains a valid mapping (must be checked)
 - Is metadata (store, true - default) - no validation, but is stored on Event
 - Is metadata (store, false) - no validation, not stored on the Event - can be used for validation

----

// Change name of generation methods

OnRaising{EventName}
OnRaised{EventName}

----

// Support:

[GenerateAggregateEvent]
public partial CustomerAggregate Deactivate();

[GenerateAggregateEvent]
public partial CustomerAggregate Reactivate();


MUST have abstract overrides for applying.

Then we can set IsActive appropriately - no more ChangeIsActive/ IsActiveChanged
