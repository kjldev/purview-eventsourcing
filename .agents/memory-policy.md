# Agent Memory Policy

Only store durable, reusable project facts:

- architecture and conventions
- stable build/test/lint commands
- project-specific rules

Never store:

- secrets or credentials
- personal/sensitive data
- transient task notes
